apiVersion: hermes.agent/v1
kind: HermesInstance
metadata:
  name: migrated-from-sibling
spec:
  image:
    repository: ghcr.io/stubbi/hermes-agent
    tag: "1.4.2"
  storage:
    persistence:
      enabled: true
      size: 20Gi
  migration:
    fromOpenClaw:
      source:
        openclawInstanceRef:
          name: my-openclaw
          namespace: agents
      mode: copy
